const routes = {
  home: '/',
  login: '/login',
  register: '/register',
  profile: '/profile',
  catalog: '/catalog',
  mapDetail: '/catalog/:id',
  checkout: '/checkout',
  logout: '/logout',
  notFound: '*',
};

export default routes;
