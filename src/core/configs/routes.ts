const routes = {
  home: '/',
  login: '/login',
  register: '/register',
  profile: '/profile',
  catalog: '/catalog',
  checkout: '/checkout',
  logout: '/logout',
  notFound: '*',
};

export default routes;
